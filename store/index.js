import modal from './modules/modal.js'

const state = () => ({
  
});

const mutations = {
  
};

const actions = {
  
};

const modules = {
    modal
};

export default {
  state,
  mutations,
  actions,
  modules,
}

